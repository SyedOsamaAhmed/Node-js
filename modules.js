const names = require("./module_params");
const sayHi = require("./utils");

const generic = require("./alternative_export_methods");
// console.log(names);

// sayHi(names.john);
// sayHi(names.peter);

console.log(generic);
